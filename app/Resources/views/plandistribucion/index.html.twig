{% extends 'base.html.twig' %}

{% block body %}
    <h1>Plandistribucion list</h1>

    <table>
        <thead>
            <tr>
                <th>Idplandistribucion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for plandistribucion in plandistribucions %}
            <tr>
                <td><a href="{{ path('plandistribucion_show', { 'id': plandistribucion.idplandistribucion }) }}">{{ plandistribucion.idplandistribucion }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('plandistribucion_show', { 'id': plandistribucion.idplandistribucion }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('plandistribucion_edit', { 'id': plandistribucion.idplandistribucion }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('plandistribucion_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
