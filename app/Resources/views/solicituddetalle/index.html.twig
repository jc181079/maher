{% extends 'base.html.twig' %}
{% use "::navbar.html.twig" %}
{% block body %}
    {% block navbar %}
        {{ parent() }}
    {% endblock %}
     <div class="jumbotron">
        <div class="container">
            <h1><strong>.:Modulo de administracion de DETALLE DE LAS SOLICITUDES</strong></h1>
            <p>aqui podra administrar los diferentes detalles de solicitudes hechos por los clientes de la empresa</p>
            <footer>
                <a class="btn btn-default" href="{{ path('clientes_new') }}">Ingresar nuevo registro</a>
            </footer>
        </div>
     </div>
     {% if app.session.flashbag.get('Mensaje') %}      
         {% for flashMessage in app.session.flashbag.get('Mensaje') %}
             <div class="alert alert-success" role="alert">
                 <span class="glyphicon glyphicon-warning-sign" ></span> 
                 {{flashMessage}}...
             </div>
         {% endfor%}
     {% endif%}
     {% if app.session.flashbag.get('Alerta') %}      
         {% for flashMessage in app.session.flashbag.get('Alerta') %}
             <div class="alert alert-danger" role="alert">
                 <span class="glyphicon glyphicon-warning-sign" ></span> 
                 {{flashMessage}}...
             </div>
         {% endfor%}
     {% endif%}
<div class="container">
         <table class="table table-bordered table-responsive">
        <thead>
            <tr>
                <th>Idsolicituddetalle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% if solicituddetalles %}
                {% for solicituddetalle in solicituddetalles %}
                    <tr>
                        <td><a href="{{ path('solicituddetalle_show', { 'id': solicituddetalle.idsolicituddetalle }) }}">{{ solicituddetalle.idsolicituddetalle }}</a></td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('solicituddetalle_show', { 'id': solicituddetalle.idsolicituddetalle }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('solicituddetalle_edit', { 'id': solicituddetalle.idsolicituddetalle }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
            {% else %}
                <tr >
                    <td colspan="7"><div class="alert alert-danger" role="alert"><span class="glyphicon glyphicon-exclamation-sign"></span> No hay regiastros en este modulo por los momentos..</div></td>
                </tr>
            {% endif%}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('solicituddetalle_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
