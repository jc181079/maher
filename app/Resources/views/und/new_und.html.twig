{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <header><h1>.:Registro de una nueva unidad de medida</h1></header>
        <div class="jumbotron">
            <div class="form-group">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="modal-footer">
                    <input class="btn btn-success" type="submit" value="Create" /> | 
                    <a class="btn btn-default" href="{{ path('und_index') }}">Back to the list</a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
div>